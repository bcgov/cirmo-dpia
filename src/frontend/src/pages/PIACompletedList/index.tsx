import ListViewRender from '../../components/public/ListViewRender';

const PIACompletedList = () => {
  return <ListViewRender showCompleted={true} title={`Completed PIAs`} />;
};

export default PIACompletedList;
