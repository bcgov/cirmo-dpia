import ListViewRender from '../../components/public/ListViewRender';

const PIAActiveList = () => {
  return <ListViewRender showCompleted={false}
  title={`Active PIA's`} />;
};

export default PIAActiveList;
